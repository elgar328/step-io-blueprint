/target
/.claude

# inferred/ holds the frozen corpus input (corpus_usage.toml) and the generated
# universal.toml — both tracked so the blueprint is browsable without running the
# tool. profiles/ (generated output profiles) is tracked for the same reason.
