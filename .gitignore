/target

# inferred/ holds both the hand-authored inputs and the generated pipeline
# outputs (ir.toml etc.) — all tracked so the blueprint is browsable without
# running the tool. Only the transient pending-gate files are ignored.
/inferred/*_pending.toml
